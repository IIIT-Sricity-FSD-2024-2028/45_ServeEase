package-lock.json

# Runtime uploads - keep local user uploads out of Git
/back-end/uploads/verification/
/back-end/uploads/tickets/
/back-end/uploads/profiles/

# Runtime state persisted by the API (availability and frontend state bridge)
/back-end/data/
